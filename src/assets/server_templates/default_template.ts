import NewServerIcon from '../icons/new_server_default.png'
import defaultChannels from './channel_templates/default_channels'

export default {
  label: 'Create my own',
  icon: NewServerIcon,
  channels: [...defaultChannels]
}
